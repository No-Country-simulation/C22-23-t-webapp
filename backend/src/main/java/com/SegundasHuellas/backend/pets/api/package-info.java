/**
 * Este paquete contiene la API pública del módulo de mascotas que está destinada a ser compartida con otros módulos.
 * Cualquier clase, interfaz o tipo que necesite ser accedido por otros módulos debe colocarse aquí.
 * <p>
 * Esto incluye:
 * - Objetos de transferencia de datos (DTO)
 * - Interfaces públicas
 * - Eventos
 * - Constantes y enumeraciones compartidas
 * <p>
 * Nota: Los detalles de implementación deben permanecer en los paquetes internos del módulo de mascotas.
 */
@NamedInterface("pets-api")
package com.SegundasHuellas.backend.pets.api;

import org.springframework.modulith.NamedInterface;